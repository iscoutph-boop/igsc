GOOGLE_APPS_SCRIPT_WEB_APP_URL=

# Apps Script V6.1 handles customer confirmations and admin notifications.
# Deploy Preview routing refreshed after validating the exact Apps Script Web App deployment ID.
# Keep the separate Netlify notification webhook disabled unless explicitly reintroduced after QA.
BOOKING_NOTIFICATION_ENABLED=false
BOOKING_NOTIFICATION_WEBHOOK_URL=
BOOKING_NOTIFICATION_WEBHOOK_SECRET=
