GOOGLE_APPS_SCRIPT_WEB_APP_URL=

# Google Apps Script V6.2 is the single authority for booking lifecycle side effects:
# CRM writes, Google Calendar updates, customer emails, and internal admin emails.
# Keep this endpoint environment-scoped (Deploy Preview for staging, Production only after approval).
