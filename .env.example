# Server-only Google Apps Script booking/CRM endpoint.
# Do not prefix this variable with VITE_ and do not expose it in browser code.
GOOGLE_APPS_SCRIPT_WEB_APP_URL=https://script.google.com/macros/s/REPLACE_WITH_DEPLOYMENT_ID/exec
