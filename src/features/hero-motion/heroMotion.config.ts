export const HERO_MOTION = {
  enabled: true,
  lightsStart: 0.52,
  hysteresis: 0.015,
  revealDiameter: "clamp(180px, 16vw, 240px)",
  sketchIntentMs: 40,
  lightsIntentMs: 70,
  lightsSettledMs: 420,
} as const;
